# Feature List

Valid states: `not_started`, `active`, `blocked`, `passing`.

Only one feature should be `active` at a time unless the user explicitly
requests otherwise.

This file is the source of truth for the current `master` branch. Historical
notes imported from `grid_map` must not be treated as implemented here unless
they appear below.

| ID | Lane | Behavior | Verification | State | Evidence |
|---|---|---|---|---|---|
| F01 | core | ORB-SLAM3 DBoW3 core build completes from repo root. | `./build.sh` | not_started | TODO |
| F02 | ros | ROS package builds through the checked-in wrapper. | `./build_ros.sh` | passing | Verified on 2026-05-25 with `./build_ros.sh`. rosbuild regenerated `ORB_SLAM3/RelocalizationStatus` and linked `Stereo`, `RGBD`, `Mono`, `Mono_Inertial`, and `Stereo_Inertial` successfully. |
| F03 | ros | The runtime wrapper starts a stereo-inertial mapping session from repo root and produces the expected side outputs for logs, resource monitoring, and bag recording. | `./run_stereo_inertial.sh mapping stereo_inertial --log` | not_started | TODO |
| F04 | docs | Governance docs match the scanned entrypoints and constraints on the current `master` branch. | Manual check against `README.md`, `build.sh`, `build_ros.sh`, `run_stereo_inertial.sh`, `CMakeLists.txt`, and `Examples_old/ROS/ORB_SLAM3/CMakeLists.txt` | passing | Verified on 2026-05-25 while migrating governance files from `grid_map` to `master` and removing branch-specific claims that do not exist on `master`. |
| F05 | ros | The runtime wrapper selects the correct `*_load.yaml` file and launches the requested localization mode without inventing extra wrapper flags. | `./run_stereo_inertial.sh localization <sensor> [--log]` and confirm the printed YAML path matches the selected sensor. | not_started | TODO |
| F11 | ros | The `Stereo` ROS wrapper publishes `/odometry` as `nav_msgs/Odometry`, using a session-relative `odom` stream derived from the externally continuous pose rather than the raw ORB-SLAM Atlas map origin: latch the first successful tracked body-frame pose as a local origin, publish subsequent poses in a ROS-style camera body frame, keep differential `twist` from consecutive fresh tracked poses, hold pose but mark twist unknown during tracking loss, and expose the TF chain as `continuous_map -> odom -> left_camera_link -> left_camera`. Low-confidence twist output is acceptable if it is explicit in covariance or equivalent metadata instead of silently reporting high-confidence motion. | `./build_ros.sh`, then run `./run_stereo_inertial.sh mapping stereo --log` or `./run_stereo_inertial.sh localization stereo --log`, confirm `/odometry` is advertised as `nav_msgs/Odometry`, confirm `header.frame_id` is `odom`, confirm `child_frame_id` is `left_camera_link`, confirm the first published pose after successful tracking is near the local origin instead of inheriting the loaded map origin directly, confirm `twist` changes over time from finite differences rather than remaining permanently zero during live or replayed motion, confirm held-pose tracking-loss samples use unknown twist covariance, and confirm the node publishes a usable TF chain for `continuous_map -> odom -> left_camera_link -> left_camera`. | blocked | Implemented in `Examples_old/ROS/ORB_SLAM3/src/ros_stereo.cc`: `/odometry` rebases pose to a latched first-valid local origin, publishes `header.frame_id = "odom"` with `child_frame_id = "left_camera_link"`, derives pose from the continuous `/robot_pose` frame, keeps `twist` tied to consecutive fresh valid tracked poses, and broadcasts `continuous_map -> odom -> left_camera_link -> left_camera`. `./build.sh` and `./build_ros.sh` passed on 2026-06-05 after the Scheme B update. Fresh runtime validation is still blocked because no live/replayed stereo runtime session was run after the change, so TF visibility, held-pose behavior, and live odometry semantics remain TODO to confirm. |
| F12 | ros | In localization mode, the `Stereo` ROS wrapper publishes `/relocalization_status` at `10 Hz`, carrying `uint64 timestamp_ns` and `string status`, where `status` uses exactly `RelocalizationRunning`, `RelocalizationSucceed`, and `RelocalizationFailed`. | `./build_ros.sh`, then run `./run_stereo_inertial.sh localization stereo --log`, confirm the package-local status message builds, confirm the topic publishes at approximately `10 Hz`, confirm payload fields are `timestamp_ns` and `status`, and confirm runtime output reaches the documented status strings during relocalization attempts. | passing | Verified on 2026-05-25 with `./build_ros.sh`, `./run_stereo_inertial.sh localization stereo --log`, `rostopic type /relocalization_status`, `rostopic hz /relocalization_status`, and `rostopic echo -n 1 /relocalization_status`. The observed topic payload used `RelocalizationSucceed` and the measured publish rate was about `10.0 Hz`. Short topic captures converged quickly and did not separately sample `RelocalizationRunning` or `RelocalizationFailed`, but the implementation only maps those three strings and the runtime logs showed repeated relocalization attempts before and after convergence. |
| F13 | ros | The `Stereo` ROS wrapper publishes `/robot_pose` as `geometry_msgs/PoseStamped` for real-time robot position feedback in an external `continuous_map` frame. After the first valid tracked pose, the topic must publish at image callback cadence even when tracking is lost by holding the last valid continuous pose, must not publish a default origin pose on tracking loss, and must preserve continuity across ordinary new-map switches without being pulled back toward `/robot_pose_slam` by loop-closure or merge corrections. `/robot_pose_tracking_ok` reports whether the current sample is fresh tracking output. `/robot_pose_slam` publishes the raw/optimized ORB-SLAM Atlas pose in `map` so loop-closure and return-to-start error can be inspected separately from the continuous control pose. | `./build_ros.sh`, then run `./run_stereo_inertial.sh mapping stereo --log` or `./run_stereo_inertial.sh localization stereo --log` with live or replayed stereo inputs, confirm `/robot_pose` is advertised as `geometry_msgs/PoseStamped`, confirm `header.frame_id` is `continuous_map`, confirm `/robot_pose_slam` is advertised as `geometry_msgs/PoseStamped`, confirm `/robot_pose_slam.header.frame_id` is `map`, confirm `/robot_pose_tracking_ok` is advertised as `std_msgs/Bool`, confirm the pose stream changes coherently during successful tracking, confirm tracking-loss periods after the first valid pose keep the `/robot_pose` publish cadence while `/robot_pose_tracking_ok` is `false`, confirm ordinary map-change increments do not spam correction logs, and confirm new-map recovery plus later loop/merge events do not pull `/robot_pose` away from its continuous trajectory. Inspect `/robot_pose_slam` separately around loop closure or return-to-start to evaluate the optimized SLAM map pose. | blocked | Updated on 2026-06-06 in `Examples_old/ROS/ORB_SLAM3/src/ros_stereo.cc`: `/robot_pose` no longer enters the gradual convergence path at all, so its behavior is now limited to continuity rebasing, tracking-loss hold-last-valid output, and raw-SLAM comparison via `/robot_pose_slam`. Fresh live/replayed runtime validation is still TODO; handed off as blocked on 2026-07-30 so F15 can become the single active feature. |
| F14 | ros | The `Stereo` ROS wrapper publishes `/robot_pose_map` as a planning-oriented `geometry_msgs/PoseStamped` stream in `planning_map`, separate from the external continuous `/robot_pose` and the raw `/robot_pose_slam` in `map`. It reads `PoseCorrection.EnableCorrectedMapPose`, `PoseCorrection.CorrectionHorizonFrames`, and `PoseCorrection.AutoEnableOnMapEvent` from the stereo YAMLs, exposes `/pose_correction/set_enabled` as a `std_srvs/SetBool` runtime toggle, tracks map-id and big-change signals through `System`, and broadcasts `planning_map -> map` so downstream planning or grid-map consumers can stay in a smooth planning frame while raw SLAM remains inspectable in `map`. On ordinary new-map switches, it must rebind `planning_map` to the last continuous pose to avoid a hard jump. On tracking loss, it must enter an unstable window and stop converging toward raw SLAM until the subsequent merge-equivalent big map event closes that window. Once stable again, big map events blend the planning pose back toward the raw SLAM pose over the configured frame horizon. Routine `map_change` increments must not repeatedly restart this correction window. | `./build_ros.sh`, then run `./run_stereo_inertial.sh mapping stereo --log` or `./run_stereo_inertial.sh localization stereo --log`, confirm `/robot_pose_map` is advertised as `geometry_msgs/PoseStamped`, confirm `/robot_pose_map.header.frame_id` is `planning_map`, confirm `/pose_correction/set_enabled` is advertised as `std_srvs/SetBool`, confirm `planning_map -> map` is visible in TF, confirm ordinary `map_change` updates do not spam gradual-correction logs, confirm map switches do not produce a hard jump in `/robot_pose_map`, confirm tracking loss blocks convergence until the subsequent merge-equivalent big event, and confirm the planning output transitions over `PoseCorrection.CorrectionHorizonFrames` instead of jumping immediately once convergence resumes. Also validate the fixed auto-enable contract by setting `PoseCorrection.EnableCorrectedMapPose` false with `PoseCorrection.AutoEnableOnMapEvent` true and confirming a big map event still starts a one-shot correction window after the unstable window closes. | blocked | Updated on 2026-06-06 in `Examples_old/ROS/ORB_SLAM3/src/ros_stereo.cc`, `Examples_old/ROS/ORB_SLAM3/MyD435i_stereo.yaml`, and `Examples_old/ROS/ORB_SLAM3/MyD435i_stereo_load.yaml`: `/robot_pose_map` remains in `planning_map`, rebases through map switches, and retains the explicit tracking-loss unstable window that suppresses convergence toward raw SLAM until the next merge-equivalent big event. Fresh ROS runtime validation is still TODO, so the feature stays blocked. |
| F10 | docs | The imported `docs/grid-map-port-plan.md` remains available as historical planning reference only and is clearly marked as non-authoritative for current `master` capabilities. | Manual check that `docs/grid-map-port-plan.md`, `AGENTS.md`, and this feature list all describe it as reference-only material. | passing | Verified on 2026-05-25 during the governance migration session. |
| F15 | evaluation | XFeat is reproduced locally in an isolated upstream checkout, its inference outputs are sanity-checked, and the RDK X5 BPU deployment path is assessed against the Bayes-e toolchain and supported-operator constraints. | Run the repository-local XFeat smoke test on two sample images, verify finite keypoints/descriptors/matches, export or isolate a fixed-shape ONNX inference core, validate it with ONNX Runtime when available, and document any OpenExplorer/BPU conversion or board-runtime blocker explicitly. | passing | Verified 2026-07-30 at upstream `e92685f`: official CPU sparse inference returned 1024 finite 64D features per image and 63 matches; the optimized fixed-shape opset-11 ONNX core passed ONNX Runtime with max raw-output error below `1.4e-5`; the split pipeline preserved both keypoint sets and all 63 match coordinate pairs. Evidence is in `xfeat_rdk_x5/artifacts/*.json` and `docs/xfeat-rdk-x5-research.md`. Actual PTQ/`.bin`/board runtime remains an explicit follow-up because `hb_mapper` was unavailable and the board SSH timed out. |
| F16 | evaluation | XFeat is evaluated offline on `2026-06-05-09-59-04.bag` using synchronized 640x544 infrared stereo frames, with an OpenCV ORB baseline and geometry-aware stereo/temporal metrics. | Read the bag without a ROS master, sample synchronized `/camera/infra1/image_raw` and `/camera/infra2/image_raw`, run XFeat and ORB on identical frames, record keypoint count, extraction latency, match count, RANSAC fundamental-matrix inliers, rectified-stereo epipolar inliers, spatial coverage, and representative match visualizations. | passing | Verified 2026-07-30 on 130 stereo and 65 adjacent temporal pairs. Both methods met the robust-pair rule on every pair. XFeat achieved 81.6% feature-grid coverage versus ORB's 56.8%, but had 15.9% fewer strict stereo inliers, 23.7% fewer temporal RANSAC inliers, and 13.40x host CPU extraction latency. During sampled `/robot_pose_tracking_ok=false` times XFeat had more strict stereo inliers and much broader coverage, but remained weaker temporally. Evidence: `docs/xfeat-bag-evaluation.md` and `xfeat_rdk_x5/artifacts/bag_2026-06-05-09-59-04/`. |
| F17 | docs | Generated runtime data and build artifacts are ignored, and the local Git object store is validated and pruned without changing source history or user worktree files. | Run `git fsck --full`, identify large reachable objects and their retaining refs, verify representative paths with `git check-ignore -v`, prune confirmed temporary/unreachable objects, then compare `git count-objects -vH`, `du -sh .git`, and `git status --short` before and after. | passing | Verified 2026-07-30. Normal branch refs retained only 0.467 GiB of blobs while the six stale internal `refs/codex` snapshots retained 30.405 GiB, primarily ignored rosbag files. `git fsck --connectivity-only` and the final `git fsck --full --no-progress` both exited 0. After deleting those stale refs, running default-safe `git gc`, and removing five files explicitly reported as `tmp_pack_*` garbage, `.git` fell from about 87 GiB to 20 GiB and `size-garbage` became zero. Codex later created one new current-session checkpoint containing about 483 MiB of blobs and no ignored bags; most of those objects are shared with normal history. Representative `git check-ignore -v` checks passed. Immediate `--prune=now` was not used, so 19.07 GiB of recent unreachable objects remain under Git's recovery grace period. An unexpected removal of ignored `bags/` files during maintenance was reversed from validated dangling tree `61fa312...`; all 15 filenames and byte sizes matched after restoration, and the index/branch refs were unchanged. |
| F18 | evaluation | The fixed-shape XFeat core is calibrated with representative frames from `2026-06-05-09-59-04.bag`, converted to an INT8 Bayes-e `.bin` with OpenExplore v1.2.6 / `hb_mapper 1.23.8`, and exercised on the RDK X5 BPU. | Require `hb_mapper checker` to place the complete core in one BPU subgraph; generate and validate 100 normalized calibration tensors; run `hb_mapper makertbin`; inspect model metadata and quantization accuracy; transfer the resulting `.bin` to `root@192.168.1.10`; run board inference and record BPU latency and output validation evidence. | passing | Verified 2026-07-31. All 33 reported nodes were assigned to one Bayes-e BPU subgraph. PTQ with 100 evenly sampled bag frames generated a 920,712-byte `.bin` built by `hb_mapper 1.23.8`. Board DNN 1.23.10 / HBRT 3.15.54 loaded it and completed 50 warmed BPU0 inferences at 13.473 ms mean. All board outputs were bit-identical to quantized-ONNX simulation; board-vs-float cosine was 0.995747, 0.990869, and 0.997176 for the three outputs. Evidence: `xfeat_rdk_x5/artifacts/ptq_board_validation_480x640.json` and `docs/xfeat-rdk-x5-research.md`. |
| F19 | evaluation | A complete board-side XFeat frontend combines CPU grayscale/normalization, Bayes-e BPU inference, sparse heatmap/NMS/top-k/descriptor sampling, and cosine matching, then compares its end-to-end latency and stereo/temporal geometry against OpenCV ORB while the host replays the recorded stereo bag over a ROS1 LAN. | Export and PTQ a native `1x1x544x640` core; run a board ROS node subscribed to the host bag topics through `ROS_MASTER_URI=http://192.168.1.3:11311`; sample identical stereo and adjacent temporal pairs; record preprocessing, BPU, postprocessing, extraction, matching, RANSAC/rectified inliers, ratios, and coverage for XFeat and ORB; require finite metrics and evidence covering the full bag. | passing | Verified 2026-07-31. Native-height PTQ placed all 33 nodes on BPU and retained >0.994 output cosine. Host `192.168.1.3` replayed the full 644-frame bag to board `192.168.1.10`; 390 records covered the expected original indices for 130 stereo and 65 temporal pairs per method. XFeat mean/median extraction was 64.85/60.00 ms versus ORB 68.67/66.34 ms, but mean stereo end-to-end latency was 185.63 versus 161.98 ms. XFeat had wider inlier coverage but fewer strict inliers; both had 100% robust-pair rate. Evidence: `xfeat_rdk_x5/artifacts/board_ros_benchmark_544x640/summary.json` and `docs/xfeat-rdk-x5-board-benchmark.md`. |
| F20 | evaluation | The RDK X5 stereo benchmark uses the board's actual BPU topology and overlaps left/right XFeat work safely: CPU preprocessing/postprocessing run concurrently while the single available BPU0 is serialized, with an equivalent parallel ORB extraction baseline. | Prove BPU1 availability with a real DNN inference and sysfs/device evidence; implement independent per-image buffers and a safe single-BPU stereo pipeline; run CPU work in bounded worker threads; verify deterministic feature/match metrics against F19; replay smoke and full bag; record extraction wall time, stage timing, pair latency, resource constraints, and evidence. | passing | Verified 2026-07-31. BPU1 inference returned `-6000001`, and only `bpu_core0` exists. Two independent XFeat/ORB instances now run left/right worker threads; one mutex serializes only BPU0 while OpenCV parallelizes CPU dense normalization, softmax and descriptor sampling. Eight CPU workers won the 1/4/8 smoke sweep. Full-bag XFeat stereo extraction wall time is 57.78 ms versus parallel ORB 74.48 ms; XFeat stereo end-to-end improved 32.4% to 125.46 ms but remains above ORB 99.38 ms. All 390 quality rows exactly match F19. Evidence: `docs/xfeat-rdk-x5-parallel-benchmark.md` and `xfeat_rdk_x5/artifacts/board_ros_benchmark_parallel_544x640/`. |
| F21 | evaluation | A geometry-guided XFeat frontend exports fixed-size official Fine Matcher variants, replaces global stereo descriptor matching with rectified row/disparity candidate search, and establishes a machine-readable production ORB-SLAM3 baseline on the RDK X5 before any SLAM integration is attempted. | Export and validate Fine Matcher ONNX models for `M=256/384/512`; require float parity and fixed input/output shapes; build the board frontend; replay `2026-06-05-09-59-04.bag` against the real S316 ORB-SLAM3 `600`-feature/`5`-level baseline; record extraction, stereo matching, valid-depth, strict-inlier and tracking timing; require XFeat mean frontend latency <= 90% of ORB, lower P90, and at least 95% of ORB geometric quality before advancing. | passing | Verified 2026-08-03 with five alternating complete board runs after 30 source-frame warmup per run. Frozen XFeat* M384 averaged `54.633/59.402 ms` frontend mean/P90 versus production ORB `70.505/81.827 ms`; board NCC-valid count was 98.54% of ORB valid depths. A common evaluator using the real S316 `Frame::ComputeStereoMatches` gave XFeat/ORB NCC points `213.94/209.68`, coverage `0.844/0.742`, PnP inliers `123.77/105.63`, 65/65 success and no fragile pair for either. Two 1000-iteration Fine runs were deterministic and task-release clean. Formal evidence: `docs/f21-gate-report.md` and `xfeat_rdk_x5/artifacts/f21_gate_final.json`; F22 is now permitted but remains `not_started`. |
| F22 | core | Hybrid stereo SLAM consumes XFeat-refined stereo depth while retaining ORB temporal tracking, MapPoint descriptors, BoW and loop closing. | Build the RDK ROS workspace, replay the S316 lawn bags `2026-08-06-09-46-39.bag`, `2026-08-06-09-48-14.bag`, and a non-destructively reindexed copy of `2026-08-06-09-50-14.bag`, and require no regression in tracking-loss count/duration or valid ORB observations; record depth/reprojection and trajectory evidence. | passing | Verified 2026-08-10 on RDK X5 at 0.5x with the production stereo gate restored to 30. On 2,496 common-timestamp frames, Hybrid preserved the exact ORB keypoint counts, had zero valid-depth regression frame, added `18.39/17.12/16.13` mean depths and `18.79/13.88/17.82` mean tracked inliers. ORB/Hybrid loss frames were `0/0`, `34/34`, and `34/0`; map counts were `1/1`, `2/2`, and `2/1`. Hybrid mean callback time was `161.85/162.41/155.92 ms` versus ORB `106.46/107.11/100.72 ms`, so this is a validated quality bridge, not an 11 Hz production configuration. Evidence: `xfeat_rdk_x5/artifacts/f22_hybrid_ab/summary.json` and matching CSV/log files. |
| F23 | core | XFeat float descriptors replace ORB in initialization and tracking with descriptor-type-aware distance, XFeat MapPoint medoids and reference-observation-only Fine Matcher refinement. | Run MH_01, MH_03 and MH_05 five times each; require 5/5 initialization, ATE/RPE <= 1.05x ORB, and no worse tracking-loss duration. | not_started | F22 passed 2026-08-10; ready for descriptor-aware initialization/tracking work. |
| F24 | core | Pure XFeat mode uses an independent float DBoW3 vocabulary and descriptor/model-tagged Atlas while retaining explicit ORB and hybrid compatibility modes. | Verify XFeat vocabulary retrieval, relocalization and loop Sim3; verify matching Atlas loads and explicit rejection of ORB/XFeat metadata mismatches; repeat the EuRoC trajectory gates. | not_started | Gated on F23 passing. |
| F26 | core | An isolated RDK-X5 RGB-D debug branch records low-overhead per-frame `TrackRGBD` stage timings and asynchronous LocalMapping queue/optimization timings, then flushes two correlation-ready CSV files only after clean shutdown. | Cross-build the debug core and ROS wrapper; verify aarch64 artifacts; replay both accepted RGB-D bags at 1.0x plus the failing bag at 0.5x; require populated frame/LocalMapping CSVs, clean shutdown and correlation-ready stage values. | passing | Verified on board 2026-08-14 after removing the new fields' accidental dependency on disabled legacy `REGISTER_TIMES`. Corrected core SHA-256 `6df2983c...efe0d9b`; all three valid runs populated both CSVs and exited 0. At 1.0x callback mean was 89.5--91.2 ms against a 90 ms period; ORB cost 49.5--52.5 ms and Local BA abort ratio was 60%--75%. The 0.5x control processed 624/624 with no loss. See `docs/rgbd-track-timing.md` and `board_rgbd_timing_test_20260814/REPORT.md` in the parent project. |
| F25 | core | A temporary stereo-tracking diagnostic build lowers the local-map inlier gate from 30 to 25 and emits near-gate pose-step and reprojection-error evidence without changing inertial thresholds or production artifacts. | Cross-build the RDK X5 core/ROS staging artifacts, replay `2026-06-05-09-59-04.bag` at `1.0x` with the live camera CPU-isolated, require complete diagnostic rows around the former loss interval, and compare tracking state, Atlas map count, pose jumps, and reprojection residuals against the threshold-30 run. | passing | Verified 2026-08-06 with ARM64 library SHA-256 `3f4de91f...e085c54f` staged separately at `/userdata/orb_slam/f25_stereo_gate25_sparse_20260806`. With viewer off, camera on CPUs 0-1 and SLAM on CPUs 2-7, `09-59-04` stayed OK for 643 callbacks and saved one 135-KF/2822-MP Atlas. Frame 523 was accepted at exactly 25 map inliers with 0.063 m/1.61 deg pose step and 1.26/2.37 px reprojection mean/P90. `09-56-06` instead split at frame 432 because reference-KF BoW matching returned only 9 matches before the local-map gate; the same threshold-30 baseline had succeeded, so this remains a scheduling/map-state repeatability issue rather than evidence for lowering the gate. Evidence: `/tmp/orb_slam3_{095606,095904}_rate1_camera_cpuiso_gate25_sparse.csv` and matching `*_gate25_sparse_diag.log`. |
