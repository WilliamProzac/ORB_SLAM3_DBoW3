# Feature List

Valid states: `not_started`, `active`, `blocked`, `passing`.

Only one feature should be `active` at a time unless the user explicitly requests otherwise.

| ID | Lane | Behavior | Verification | State | Evidence |
|---|---|---|---|---|---|
| F01 | core | ORB-SLAM3 DBoW3 core build completes from repo root. | `./build.sh` | not_started | TODO |
| F02 | ros | ROS package builds through the checked-in wrapper. | `./build_ros.sh` | not_started | TODO |
| F03 | ros | Stereo-inertial mapping mode starts through the runtime wrapper. | `./run_stereo_inertial.sh mapping stereo_inertial --log` | not_started | TODO |
| F04 | docs | Harness governance docs match the scanned repo scripts and entrypoints on this branch. | Manual check against `README.md`, `build.sh`, `build_ros.sh`, `run_stereo_inertial.sh`, `CMakeLists.txt`, and `Examples_old/ROS/ORB_SLAM3/CMakeLists.txt` | passing | Verified by static scan on `grid_map` branch during the 2026-05-14 documentation session. |
| F10 | docs/ros | Read `/home/ywl/Project/ORB-SLAM3-GRID-MAP` in detail, identify how grid-map export is implemented there, and produce a repo-local migration plan that names the target ROS integration path under `Examples_old/ROS/ORB_SLAM3/` before any implementation begins. | Manual check that `docs/grid-map-port-plan.md` cites the source implementation files in `/home/ywl/Project/ORB-SLAM3-GRID-MAP` and names the intended target files under `Examples_old/ROS/ORB_SLAM3/`. | passing | Verified on 2026-05-15 by completing `docs/grid-map-port-plan.md` with source file citations, source data-flow notes, and intended target files under `Examples_old/ROS/ORB_SLAM3/`. |
| F11 | ros | Grid-map export can publish a planner-readable occupancy representation from the `Stereo` ROS wrapper before any pose-interface follow-up work begins. | Manual procedure: run `./build_ros.sh`, then run `./run_stereo_inertial.sh mapping stereo --log`, and during the ROS session confirm `/grid_map` publishes `nav_msgs/OccupancyGrid` with `rostopic echo -n 1 /grid_map/info` or equivalent topic inspection. | passing | Verified on 2026-05-15 by building the ROS package, starting the stereo wrapper, and confirming `/grid_map` is advertised as `nav_msgs/OccupancyGrid` during runtime topic inspection. |
| F12 | ros | The `Stereo` ROS wrapper publishes one stable `geometry_msgs/PoseStamped` interface on `/grid_map/pose` for grid-map consumers, representing the tracked left-camera pose in the `map` frame without expanding into odometry semantics. | Manual procedure: run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./build_ros.sh`, then run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./run_stereo_inertial.sh mapping stereo --log`, and during the ROS session confirm `/grid_map/pose` is advertised, `rostopic type /grid_map/pose` reports `geometry_msgs/PoseStamped`, `rostopic echo -n 1 /grid_map/pose` returns a live message, and the echoed `header.frame_id` is `map`. | passing | Implemented on 2026-05-17 in `ros_stereo.cc` by publishing `/grid_map/pose` as `geometry_msgs/PoseStamped` from `Tcw.inverse()` on valid tracked frames. `./build_ros.sh` passed, bounded runtime inspection confirmed `/grid_map/pose` was advertised with type `geometry_msgs/PoseStamped`, and the user completed manual runtime validation on 2026-05-17 confirming the pose stream can be extracted successfully from `/grid_map/pose` in the `map` frame. |
| F13 | ros | Grid-map state is fully rebuilt or refreshed after loop closure so long-running occupancy output does not keep stale pre-closure accumulation. | Manual procedure: run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./build_ros.sh`, then run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./run_stereo_inertial.sh mapping stereo --log`, provoke a loop closure during the stereo mapping session, and confirm from runtime logs plus topic inspection that the stereo grid-map path triggers a full rebuild from active-map keyframes after `System::MapChanged()` instead of only continuing pre-closure incremental accumulation. | blocked | Implemented on 2026-05-15 by adding active-map keyframe export in `System` and a `System::MapChanged()`-driven full rebuild path in `ros_stereo.cc`; `./build.sh` and `./build_ros.sh` passed, and a bounded `mapping stereo --log` runtime again confirmed `/grid_map` as `nav_msgs/OccupancyGrid`, but no loop-closure-triggered rebuild log was observed within the available validation window. |
| F14 | ros | Grid-map occupancy classification uses obstacle height, and obstacles lower than `0.50 m` are treated as blocking cells in the occupancy output. | TODO: add runtime or fixture-based validation that injects map points above and below the `0.50 m` threshold and confirms the grid map marks the affected cells as occupied according to the feature definition. | not_started | TODO |
| F15 | ros | The ROS wrapper publishes a generalized live ego-motion interface such as `nav_msgs/Odometry` or another richer pose contract with explicit downstream semantics beyond the minimal `/grid_map/pose` consumer interface from `F12`, including any future `child_frame_id`, velocity, or covariance fields. | TODO: add runtime validation that starts the selected ROS wrapper and confirms the generalized odometry or richer pose topic is advertised with the expected message type and semantics. | not_started | TODO |
| F16 | ros | Stereo grid-map coverage grows dynamically beyond the initial bounds instead of discarding out-of-range cells, the published occupancy grid uses `0.50 m` resolution, and expansion logs report estimated grid storage so runtime memory growth is visible. | Manual procedure: run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./build_ros.sh`, then run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./run_stereo_inertial.sh mapping stereo --log`, confirm `/grid_map` remains `nav_msgs/OccupancyGrid`, confirm `/grid_map/info/resolution` or equivalent topic inspection reports `0.50`, and if the runtime trajectory extends beyond the initial extent, confirm the node logs grid expansion with estimated storage and `/grid_map/info` width or height increases instead of clipping points at the old fixed bounds. | passing | Verified on 2026-05-15 by rebuilding the ROS package, running `mapping stereo --log`, confirming `/grid_map/info` reported `resolution: 0.5`, and observing repeated `Grid map expanded ... approx storage ... MiB` logs as width and height grew beyond the initial `40 x 42` window. |
| F17 | ros | Stereo grid-map ignores map-point obstacle contributions beyond a configurable planar distance from the current accumulation pose so outlier-driven far points do not expand the occupancy grid. | Manual procedure: run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./build_ros.sh`, then run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./run_stereo_inertial.sh mapping stereo --log`, confirm `/grid_map` remains `nav_msgs/OccupancyGrid`, and confirm runtime logs show the stereo grid-map path ignoring distant map points beyond the configured threshold while continuing to publish expansion or initialization metadata normally. | passing | Verified on 2026-05-15 by rebuilding the ROS package, running `mapping stereo --log`, confirming `/grid_map` remained `nav_msgs/OccupancyGrid`, and observing repeated `Grid map ignored ... beyond 15 m planar range ...` logs while the same bounded runtime expanded only to `56 x 58` cells and about `0.03 MiB` instead of the earlier outlier-driven multi-hundred-MiB grid growth. |
| F18 | ros | Grid-map projection uses an IMU gravity-aligned plane instead of assuming the SLAM map-frame `x-z` plane is level, while preserving the current single-node occupancy-grid integration style. | Manual procedure: run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./build_ros.sh`, then run `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./run_stereo_inertial.sh mapping stereo --log`, confirm `/grid_map` still publishes `nav_msgs/OccupancyGrid`, confirm runtime logs show fallback and/or gravity-aligned projection state transitions for the stereo grid-map path, inspect `/grid_map/info` to confirm metadata still publishes in the `map` frame, and during a live tilt test confirm the occupancy grid remains aligned with gravity rather than the raw map-frame `x-z` assumption. | blocked | Implemented on 2026-05-16 in `ros_stereo.cc` by adding an IMU subscriber, a ROS-side gravity estimator that reads `IMU.T_b_c1`, gravity-aligned plane projection for obstacle/ray/bounds/rebuild paths, rotated `OccupancyGrid` origin metadata, and explicit fallback logging. `PYTHONPATH=/opt/ros/noetic/lib/python3/dist-packages:/usr/lib/python3/dist-packages:$PYTHONPATH ./build_ros.sh` passed, bounded `mapping stereo --log` runtimes still advertised `/grid_map` as `nav_msgs/OccupancyGrid`, and runtime logs confirmed fallback to map-frame `x-z` when gravity samples were unavailable, but no gravity-aligned activation log or tilt-test correctness evidence was captured. |
